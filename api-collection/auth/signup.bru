meta {
  name: Signup User
  type: http
}

post {
  url: {{base_url}}/api/auth/signup
  body: json
}

headers {
  Content-Type: application/json
}

body {
  json {
    {
      "email": "{{email}}",
      "password": "{{password}}",
      "full_name": "{{full_name}}"
    }
  }
}
